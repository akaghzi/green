<%= form_with(model: notice, local: true) do |form| %>
  <% if notice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(notice.errors.count, "error") %> prohibited this notice from being saved:</h2>

      <ul>
      <% notice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :property_id %>
    <%= form.text_field :property_id, id: :notice_property_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :notice_user_id %>
  </div>

  <div class="field">
    <%= form.label :subject %>
    <%= form.text_field :subject, id: :notice_subject %>
  </div>

  <div class="field">
    <%= form.label :detail %>
    <%= form.text_area :detail, id: :notice_detail %>
  </div>

  <div class="field">
    <%= form.label :publish_at %>
    <%= form.date_select :publish_at, id: :notice_publish_at %>
  </div>

  <div class="field">
    <%= form.label :expire_at %>
    <%= form.date_select :expire_at, id: :notice_expire_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
